# PostgreSQL connection settings
PGHOST=localhost
PGPORT=5432
PGDATABASE=discover_guadeloupe
PGUSER=postgres
PGPASSWORD=postgres

# API server port
PORT=3001

# CORS – comma-separated list of allowed origins (optional)
ALLOWED_ORIGINS=http://localhost:5173
